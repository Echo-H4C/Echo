# 1. 취약점 정의

XSS는 공격자가 웹 리소스에 악성 스크립트를 삽입해 이용자의 웹 브라우저에서 해당 스크립트가 실행되도록 하는 취약점으로, 해당 취약점을 통해 공격자는 특정 계정의 세션 정보를 탈취할 수 있다.

공격자가 XSS 취약점이 존재하는 웹 페이지에 악성 스크립트를 삽입하고, 이용자가 악성 스크립트가 포함된 페이지를 방문하면 공격자가 삽입한 악성 스크립트가 실행된다.

해당 취약점은 SOP(Same Origin Policy) 보안 정책이 등장하면서 서로 다른 오리진에서는 정보를 읽는 행위가 이전에 비해 힘들어졌다. 그러나 이를 우회하는 다양한 기술이 소개되면

# 2. 취약점 점검 방법

# 3. 대응방안
