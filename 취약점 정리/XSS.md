# 1. 취약점 정의

XSS는 공격자가 웹 리소스에 악성 스크립트를 삽입해 이용자의 웹 브라우저에서 해당 스크립트가 실행되도록 하는 취약점으로, 해당 취약점을 통해 공격자는 특정 계정의 세션 정보를 탈취할 수 있다.

공격자가 XSS 취약점이 존재하는 웹 페이지에 악성 스크립트를 삽입하고, 이용자가 악성 스크립트가 포함된 페이지를 방문하면 공격자가 삽입한 악성 스크립트가 실행된다.

해당 취약점은 SOP(Same Origin Policy) 보안 정책이 등장하면서 서로 다른 오리진에서는 정보를 읽는 행위가 이전에 비해 힘들어졌다. 그러나 이를 우회하는 다양한 기술이 소개되면서 XSS 공격은 지속되고 있다.

> [!TIP]
> 동일 출처 정책(Same Origin Policy)은 어떤 출처에서 불러온 문서나 스크립트가 다른 출처에서 가져온 리소스와 상호작용할 수 있는 방법을 제한하는 중요한 보안 메커니즘이다.

XSS는 발생 형태에 따라 Stored XSS, Reflected XSS, DOM-based XSS로 나뉜다.

* Stored XSS : XSS에 사용되는 악성 스크립트가 서버에 저장되고 서버의 응답에 담겨오는 XSS
* Reflected XSS : XSS에 사용되는 악성 스크립트가 URL에 삽입되고 서버의 응답에 담겨오는 XSS
* DOM-based XSS : XSS에 사용되는 악성 스크립트가 URL Fragment에 삽입되는 XSS (Fragment는 서버 요청/응답에 포함되지 않는다)

## 3.1. Stored XSS

Stored XSS는 서버의 데이터베이스 또는 파일 등의 형태로 저장된 악성 스크립트를 조회할 때 발생하는 XSS이다. 대표적으로 게시물과 댓글에 악성 스크립트를 포함해 업로드하는 방식이 있다. 게시물은 불특정 다수에게 보여지기 때문에 해당 기능에서 XSS 취약점이 존재할 경우 높은 파급력을 가진다.

## 3.2. Reflected XSS

Reflected XSS는 서버가 악성 스크립트가 담긴 요청을 출력할 때 발생한다. 대표적으로 게시판 서비스에서 작성된 게시글을 조회하기 위한 검색창에서 스크립트를 포함해 검색하는 방식이 있다. 이용자가 게시물을 검색하면 서버에서는 검색 결과를 이용자에게 반환한다. 일부 서비스에서는 검색 결과를 응답에 포함하는데, 검색 문자열에 악성 스크립트가 포함되어 있다면 Reflected XSS가 발생할 수 있다.

Reflected XSS는 Stored XSS와는 다르게 URL과 같은 이용자의 요청에 의해 발생한다. 따라서 공격을 위해서는 다른 이용자를 악성 스크립트가 포함된 링크에 접속하도록 유도해야 한다. 이용자에게 링크를 직접 전달하는 방법은 악성 스크립트 포함 여부를 이용자가 눈치챌 수 있기 떄문에 주로 Click Jacking 또는 Open Redirect 등 다른 취약점과 연계하여 사용한다.

# 2. 취약점 점검 방법

게시글을 작성할 때나 파라미터 입력 값이 응답에 포함되는 경우 입력 값에 스크립트를 삽입하고 실제로 실행되는지 확인한다.

# 3. 대응방안

## 3.1. 파라미터에 필터링 적용

파라미터 입력 값에 스크립트 실행에 필요한 특수문자(<, >,'," 등)와 문자열(script, on, img, svg 등)을 필터링한다.

## 3.2. HTML Entity로 치환

스크립트 실행에 필요한 특수문자를 HTML Entity로 치환하는 방법도 있다. <는 스크립트의 시작을 표시하는 특수문자로 사용되지만, 이를 HTML Entity로 치환하면 특별한 의미가 없는 < 특수문자로 인식되어 스크립트의 시작을 표시하는 특수문자로 인식되지 않는다.


# 4. 참고
- https://portswigger.net/web-security/cors/same-origin-policy
- https://developer.mozilla.org/en-US/docs/Web/URI/Reference/Fragment
